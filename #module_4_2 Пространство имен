#module_4_2 Пространство имен

#создаём функцию test
def test_function():
    #создаём функцию внутри функции
    def inner_function():
        print("Я в области видимости функции test_function")
    #вызываем внутреннюю функцию
    inner_function()

#тестовая функция отрабатывает штатно
test_function()

#при попытке вызова внутренней функции программа не находит её имени в глобальной области видимости (и не находит во встроенной) 
inner_function()
